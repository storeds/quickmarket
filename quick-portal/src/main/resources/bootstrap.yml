spring:
  application:
    name: quickmarket-portal
  cloud:
      nacos:
        config:
          server-addr: 192.168.96.132:8848 #配置中心的地址
          file-extension: yml #配置文件结尾的配置
          shared-dataids: quickmall-portal.yml #商城公共配置
        discovery:
          server-addr: localhost:8848
  profiles:
    active: dev

